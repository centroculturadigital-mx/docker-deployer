<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ pageTitle }}</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>

    <main>
        <h1>Containers</h1>
        <p>List of all the services...</p>
        {{#if hasContainers }}
        <div class="grid">
            {{#each containers}}
            <article>
                <header>
                    <h1>
                        {{#each this.Names }}
                            <a href="/update-image/{{../Id}}">{{this}}</a>
                        {{/each}}
                    </h1>
                </header>
            </article>
            {{/each}}
        </div>
        {{else}}
        <h1>No containers Found</h1>
        {{/if}}
    </main>
</body>

</html>